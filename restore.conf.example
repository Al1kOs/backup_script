# Пример конфигурации для восстановления системы
# Скопируйте этот файл в restore.conf и отредактируйте под ваши требования

# Файл бэкапа (обязательно)
BACKUP_FILE="./backups/system_backup_20231201_120000.img.gz"

# Целевое устройство для восстановления (обязательно)
TARGET_DEVICE="/dev/sda"

# Новые сетевые настройки (необязательно)
# Если не указаны, сетевые настройки не изменяются

# Новый hostname
NEW_HOSTNAME="newserver"

# Новый IP адрес
NEW_IP="192.168.1.200"

# Новая маска подсети
NEW_NETMASK="255.255.255.0"

# Новый шлюз
NEW_GATEWAY="192.168.1.1"

# Новые DNS серверы (через запятую)
NEW_DNS="8.8.8.8,8.8.4.4"

# Дополнительные параметры
# Установите в true, чтобы пропустить настройку сети
SKIP_NETWORK_CONFIG=false

# Комментарии:
# - BACKUP_FILE должен указывать на существующий файл бэкапа
# - TARGET_DEVICE должен быть корректным блочным устройством
# - NEW_IP должен быть доступным IP адресом в вашей сети
# - NEW_GATEWAY должен быть доступным шлюзом
# - NEW_DNS должны быть доступными DNS серверами 